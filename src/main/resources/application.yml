spring:
  application:
    name: CounsellorsPortal
  datasource:
    url: jdbc:mysql://localhost:3306/counsellors_portal
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
  main:
    banner-mode: off
server:
  port: 9090